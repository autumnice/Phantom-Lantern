# Fixtures

用于并行开发的最小 fixtures 集合：

- `plan.success.json`：大纲成功响应
- `image.success.json`：图片成功响应（最小占位图 base64）
- `batch.partial-success.json`：批量图片部分成功（用于前端错误处理验收）
- `error.ai_generation_failed.json`：统一错误格式示例
- `error.validation.json`：参数校验错误示例
