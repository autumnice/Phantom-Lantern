# NanoDeck AI - 前端设计系统

## 设计理念

### 视觉语言
复古未来主义（Retro-futuristic）- 融合经典排版美学与未来科技元素，创造既现代又永恒的视觉验。

### 字体系统
- **标题字体**: Orbitron (几何、未来感的显示字体)
- **正文字体**: Space Grotesk (现代、清晰的无衬线字体)
- **等宽字体**: JetBrains Mono (用于代码和数据)

### 色彩方案
```css
:root {
  /* 主色调 */
  --color-bg-primary: #0a0e1a;    /* 深蓝灰背景 */
  --color-bg-secondary: #1a1f2e;  /* 卡片和容器背景 */
  --color-bg-tertiary: #252b3c;   /* 输入框和按钮背景 */

  /* 强调色 */
  --color-accent-primary: #00d4ff; /* 电光蓝 - 主要交互 */
  --color-accent-secondary: #ff2a6d; /* 热粉 - 重要按钮和状态 */
  --color-accent-tertiary: #05ffa1; /* 荧光绿 - 成功状态 */

  /* 文本颜色 */
  --color-text-primary: #e0e0e0;   /* 主要文本 */
  --color-text-secondary: #a0a0a0; /* 次要文本 */
  --color-text-muted: #606060;     /* 禁用文本 */

  /* 状态颜色 */
  --color-success: #05ffa1;
  --color-warning: #ffb800;
  --color-error: #ff2a6d;
  --color-info: #00d4ff;
}
```

### 间距系统
```css
--space-xs: 0.25rem;   /* 4px */
--space-sm: 0.5rem;    /* 8px */
--space-md: 1rem;      /* 16px */
--space-lg: 1.5rem;    /* 24px */
--space-xl: 2rem;      /* 32px */
--space-2xl: 3rem;     /* 48px */
--space-3xl: 4rem;     /* 64px */
```

### 阴影和光效
```css
--glow-primary: 0 0 20px rgba(0, 212, 255, 0.5);
--glow-secondary: 0 0 20px rgba(255, 42, 109, 0.5);
--shadow-card: 0 4px 6px rgba(0, 0, 0, 0.3);
--shadow-modal: 0 10px 25px rgba(0, 0, 0, 0.5);
```

### 动画曲线
```css
--ease-out: cubic-bezier(0.25, 0.46, 0.45, 0.94);
--ease-in-out: cubic-bezier(0.455, 0.03, 0.515, 0.955);
--duration-fast: 150ms;
--duration-normal: 300ms;
--duration-slow: 500ms;
```

## 组件设计

### 1. 主页面布局

**网格系统**:
- 12列网格，最大宽度1200px
- 左右边距各24px
- 垂直间距使用24px倍数

**背景效果**:
- 动态网格线作为装饰
- 微妙的径向渐变光晕
- 浮动的几何形状

### 2. 导航栏

**设计特点**:
- 半透明背景，毛玻璃效果
- Logo使用Orbitron字体，带有霓虹光效
- 导航项目悬停时显示下划线光效

### 3. 步骤指示器

**视觉表现**:
- 横向排列的步骤点
- 当前步骤显示脉冲动画
- 已完成步骤显示荧光绿勾选
- 连接线使用渐变效果

### 4. 表单组件

**输入框**:
- 深色背景，边框在聚焦时发光
- 占位符文本使用次要文本颜色
- 错误状态显示红色光效

**按钮**:
- 主要按钮：电光蓝背景，悬停时增强光效
- 次要钮：透明背景，边框发光
- 危险按钮：热粉背景

**下拉选择**:
- 自定义下拉箭头图标
- 选项悬停时高亮
- 支持键盘导航

### 5. 幻灯片预览组件

**卡片设计**:
- 圆角矩形，带有阴影
- 图片区域显示占位符或生成的图片
- 状态指示器（生成中、已完成、错误）
- 悬停时显示操作按钮

**网格布局**:
- 响应式网格，根据屏幕大小调整列数
- 卡片之间有充足的间距

### 6. 加载指示器

**AI生成动画**:
- 脉冲圆点，使用不同颜色表示不同步骤
- 文字描述当前进行中的操作
- 进度条带有渐变光效

### 7. 模态框

**设计特点**:
- 居中显示，背景半透明遮罩
- 从中心缩放出现的动画
- 关闭按钮带有悬停效果
- 支持ESC键关闭

## 动画和交互

### 页面加载动画
1. Logo从上方滑入，带有弹性效果
2. 标题文字逐字显示
3. 背景元素淡入

### 过渡动画
- 步骤切换使用滑动效果
- 组件状态变化使用淡入淡出
- 按钮点击有微妙的缩放反馈

### 微交互
- 输入框聚焦时边框发光
- 按钮悬停时轻微上浮
- 卡片悬停时显示阴影扩展
- 图标悬停时旋转或变色

## 响应式设计

### 断点
- Mobile: < 640px
- Tablet: 640px - 1024px
- Desktop: > 1024px

### 适配策略
- 移动端使用单列布局
- 平板端使用两列布局
- 桌面端使用多列网格
- 触摸目标最小44px

## 可访问性

### 颜色对比
- 所有文本与背景对比度≥4.5:1
- 大文本对比度≥3:1

### 键盘导航
- 所有交互元素支持Tab键导航
- 聚焦状态清晰可见

### 屏幕阅读器
- 使用语义化HTML
- 提供适当的ARIA标签
- 图片有alt属性

## 性能优化

### 图片优化
- 使用WebP格式
- 懒加载非关键图片
- 提供多种分辨率

### 动画优化
- 使用transform和opacity属性
- 避免触发重排和重绘
- 使用will-change提示浏览器

### 代码分割
- 按路由分割代码
- 懒加载非关键组件